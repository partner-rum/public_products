# Локальные настройки инструментов
.claude/
.idea/

# Креды бэкофиса и сырьё выгрузки — не публиковать
.bo_creds
.tg_creds
placements_raw.json

# Python
__pycache__/
*.pyc
.venv/
venv/

# ОС
Thumbs.db
.DS_Store

# Внутренний скрипт выгрузки (адреса/схема бэкофиса) — держим локально
fetch_placements.py

# Внутренняя документация проекта и локальный конфиг MCP — не публиковать
CLAUDE.md
.mcp.json
_demo/

# Внутренний инструментарий прайсинга и рыночные выгрузки — НЕ в публичный репозиторий
Floater_tool.py
floaters_dashboard.png
*.xlsx

# artefacts of the impeccable design skill (local only)
.impeccable/

# Макеты печатного дайджеста — черновики, наружу не публикуем
_digest/

# Локальные авто-обновления по расписанию (Планировщик задач) — не публикуем:
# переоценка Bid из бэкофиса и сбор ставок для скринера
auto_refresh_*.py
_auto_refresh.log
_auto_refresh.notify
_auto_refresh.lock

# Временные рендеры и черновики (underscore-префикс): макеты промо, og-шаблоны,
# тестовые PDF. Служебное, наружу не публикуем — и не попадёт под `git add *.html`.
_*.html
_*.png
_*.pdf
# Данные к стендам вариантов: _desk-data.js под правила выше не попадал и висел
# незакоммиченным — то есть мог уехать в публичный репо с `git add -A`.
_*.js

# Черновик пользователя — не трогать и не коммитить
china-tech.html

# Конфиг деплоя воркера: id KV-namespace и биндингов — внутренняя инфраструктура
wrangler.toml
.wrangler/

# Скринер ликвидности: коллектор ставок (локальный, как fetch_placements.py)
fetch_rates.py

# Локальные рабочие заметки (underscore-префикс): планы кампаний, черновики.
# Наружу не публикуем — в них бюджеты, цены каналов и внутренние решения.
_*.md


# Продуктовая карта для дизайн-скилла — внутренняя кухня, репозиторий публичный
PRODUCT.md

# Выгрузки аналитики (Метрика и пр.): репозиторий ПУБЛИЧНЫЙ, поведение посетителей
# и метки сейлзов в него попадать не должны. .xlsx игнорируется выше, csv — нет.
_metrika/
*.csv

# Стенды дизайна (локальные, в репозиторий не идут)
_*.html
_*.js

# Метрика: токен OAuth (область только «Просмотр статистики») и локальный
# коллектор отчётов. Токен читается из env METRIKA_TOKEN, файл — запасной путь.
.metrika_token*
fetch_metrika.py
